import React, { useState, useEffect } from 'react';
import { 
  BarChart2, 
  Code, 
  PieChart, 
  TrendingUp, 
  Mail, 
  Linkedin, 
  ExternalLink, 
  Download,
  Menu,
  X,
  ChevronRight,
  Brain
} from 'lucide-react';

// --- DATA FROM CV (Moved outside for access in hooks) ---

const projects = [
  {
    id: 1,
    title: 'Retail Sales Dashboard',
    category: 'Visualization',
    tags: ['Tableau', 'Data Cleaning', 'KPIs'],
    description: 'Conducted end-to-end analysis of retail sales data. Cleaned and transformed data to identify top 10 customers and seasonality trends.',
    metric: 'Strategic Insights Generated',
    color: 'bg-orange-500',
    link: 'https://public.tableau.com/app/profile/thapelo.mohaleamalla/viz/RetailSales_17630346773280/Dashboard1'
  },
  {
    id: 2,
    title: 'TVH Hackathon Web App',
    category: 'Leadership',
    tags: ['Agile', 'Project Mgmt', 'Team Lead'],
    description: 'Led a team to develop a web app for township entrepreneurs. Managed scope, sprints, and ensured timely delivery of the MVP to judges.',
    metric: 'MVP Delivered on Time',
    color: 'bg-blue-600',
    link: 'https://patheon.netlify.app/'
  },
  {
    id: 3,
    title: 'HR Analytics Dashboard',
    category: 'Visualization',
    tags: ['Power BI', 'HR Metrics', 'Analytics'],
    description: 'Designed interactive dashboards to track attrition, satisfaction, and workforce demographics, enabling stakeholders to identify trends quickly.',
    metric: 'Workforce Trends Identified',
    color: 'bg-purple-500',
    link: 'https://public.tableau.com/app/profile/thapelo.mohaleamalla/viz/HRdashboard_17574186387630/HRAnalyticsDashboard'
  },
  {
    id: 4,
    title: 'Bellabeat Case Study',
    category: 'Analysis',
    tags: ['R', 'Spreadsheets', 'Strategy'],
    description: 'Analyzed smart device usage data to identify growth opportunities. Revealed key trends in activity patterns and sleep efficiency to inform a fashion-first retention strategy.',
    metric: '3-Pillar Strategy Proposed',
    color: 'bg-rose-500',
    link: 'https://github.com/MohalTheProgramer/Case-Study-Unlocking-Growth-Opportunities-for-Bellabeat'
  },
];

const skills = [
  { name: 'Tableau / Power BI', level: 90, category: 'Viz' },
  { name: 'SQL & Database Mgmt', level: 85, category: 'Database' },
  { name: 'Python (Pandas)', level: 80, category: 'Code' },
  { name: 'R', level: 75, category: 'Code' },
  { name: 'Java', level: 70, category: 'Code' },
  { name: 'Data Storytelling', level: 95, category: 'Analysis' },
  { name: 'Requirements Gathering', level: 90, category: 'Analysis' },
];

const timeline = [
  {
    role: 'Deputy Secretary',
    company: 'Hacker Society',
    period: 'Jan 2025 - Present',
    desc: 'Managing administrative tasks and tracking action items. Increased event attendance by 10% through data-driven engagement campaigns.'
  },
  {
    role: 'Google Data Analytics Professional',
    company: 'Coursera',
    period: 'Dec 2025',
    desc: 'Mastered the data analysis lifecycle (Ask, Prepare, Process, Analyze, Share, Act). Gained proficiency in R (Ggplot2), SQL, and Tableau for data cleaning and visualization, while developing strong skills in data ethics and stakeholder communication.'
  },
  {
    role: 'Tableau Student',
    company: 'Skill Up',
    period: '2025',
    desc: 'Built interactive dashboards and learned to transform complex datasets into actionable insights.'
  },
  {
    role: 'Data Fundamentals',
    company: 'IBM',
    period: 'May 2024',
    desc: 'Gained foundational knowledge in data analytics, visualization, and data-driven decision making.'
  },
];

const stats = [
  { label: 'Certifications', value: '3' },
  { label: 'Projects Completed', value: '6+' },
  { label: 'Hackathons', value: '1' },
  { label: 'Graduation', value: '2026' },
];

const filterCategories = ['All', 'Visualization', 'Leadership', 'Analysis'];

const Portfolio = () => {
  const [isMenuOpen, setIsMenuOpen] = useState(false);
  const [activeFilter, setActiveFilter] = useState('All');
  const [scrolled, setScrolled] = useState(false);
  
  // Hero Section Slideshow State
  const [heroProjectIndex, setHeroProjectIndex] = useState(0);
  const [heroFade, setHeroFade] = useState(true);

  // Handle scroll for navbar styling
  useEffect(() => {
    const handleScroll = () => {
      setScrolled(window.scrollY > 50);
    };
    window.addEventListener('scroll', handleScroll);
    return () => window.removeEventListener('scroll', handleScroll);
  }, []);

  // Cycle through projects in Hero section
  useEffect(() => {
    const interval = setInterval(() => {
      setHeroFade(false); // Start fade out
      setTimeout(() => {
        setHeroProjectIndex((prev) => (prev + 1) % projects.length); // Change project
        setHeroFade(true); // Fade in
      }, 500); // Wait 500ms for fade out to complete
    }, 5000); // Change every 5 seconds

    return () => clearInterval(interval);
  }, []);

  const scrollToSection = (id) => {
    const element = document.getElementById(id);
    if (element) {
      element.scrollIntoView({ behavior: 'smooth' });
      setIsMenuOpen(false);
    }
  };

  const filteredProjects = activeFilter === 'All' 
    ? projects 
    : projects.filter(p => p.category === activeFilter);

  // Theme Constants (Dark Mode Locked)
  const bgMain = 'bg-slate-950';
  const bgCard = 'bg-slate-900';
  const textMain = 'text-slate-100';
  const textMuted = 'text-slate-400';
  const border = 'border-slate-800';
  const navBg = 'bg-slate-950/90';

  const currentHeroProject = projects[heroProjectIndex];

  return (
    <div className={`min-h-screen font-sans transition-colors duration-300 ${bgMain} text-slate-200 selection:bg-blue-500 selection:text-white`}>
      
      {/* --- NAVIGATION --- */}
      <nav className={`fixed w-full z-50 transition-all duration-300 ${scrolled ? `${navBg} backdrop-blur-md shadow-sm py-4` : 'bg-transparent py-6'}`}>
        <div className="max-w-6xl mx-auto px-6 flex justify-between items-center">
          <div className="font-bold text-xl tracking-tight flex items-center gap-2">
            <div className="w-8 h-8 bg-blue-600 rounded-lg flex items-center justify-center text-white">
              <BarChart2 size={20} />
            </div>
            <span className={textMain}>Thapelo<span className="text-blue-600">.io</span></span>
          </div>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            {['About', 'Skills', 'Projects', 'Experience'].map((item) => (
              <button 
                key={item} 
                onClick={() => scrollToSection(item.toLowerCase())}
                className={`${textMuted} hover:text-blue-600 font-medium text-sm transition-colors`}
              >
                {item}
              </button>
            ))}
            
            <button 
              onClick={() => scrollToSection('contact')}
              className={`bg-blue-600 hover:bg-blue-700 text-white px-5 py-2 rounded-full font-medium text-sm transition-colors`}
            >
              Contact Me
            </button>
          </div>

          {/* Mobile Toggle */}
          <div className="md:hidden flex items-center gap-4">
            <button className={textMain} onClick={() => setIsMenuOpen(!isMenuOpen)}>
              {isMenuOpen ? <X size={24} /> : <Menu size={24} />}
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isMenuOpen && (
          <div className={`absolute top-full left-0 w-full ${bgCard} shadow-lg py-4 px-6 md:hidden flex flex-col gap-4 border-t ${border}`}>
            {['About', 'Skills', 'Projects', 'Experience', 'Contact'].map((item) => (
              <button 
                key={item} 
                onClick={() => scrollToSection(item.toLowerCase())}
                className={`text-left py-2 border-b ${border} ${textMuted} font-medium`}
              >
                {item}
              </button>
            ))}
          </div>
        )}
      </nav>

      {/* --- HERO SECTION --- */}
      <section id="about" className="pt-32 pb-20 px-6 relative overflow-hidden">
        {/* Background Gradient */}
        <div className={`absolute top-0 right-0 w-1/2 h-full bg-gradient-to-l -z-10 from-blue-900/20 to-transparent`}></div>
        
        <div className="max-w-6xl mx-auto grid md:grid-cols-2 gap-12 items-center">
          <div>
            <div className={`inline-flex items-center gap-2 px-3 py-1 rounded-full bg-blue-900/30 text-blue-300 text-sm font-semibold mb-6`}>
              <span className="relative flex h-2 w-2">
                <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-blue-400 opacity-75"></span>
                <span className="relative inline-flex rounded-full h-2 w-2 bg-blue-500"></span>
              </span>
              Open to Opportunities
            </div>
            <h1 className={`text-5xl md:text-6xl font-bold leading-tight mb-6 ${textMain}`}>
              Transforming Data into <span className="text-blue-600">Strategic Insights</span>.
            </h1>
            <p className={`text-lg ${textMuted} mb-8 leading-relaxed max-w-lg`}>
              Hi, I'm Thapelo Mohaleamalla. A Google Certified Data Analyst & Business Analyst skilled in Tableau, Power BI, Python, R and SQL. 
              I specialize in data storytelling and building interactive dashboards that drive decision-making.
            </p>
            <div className="flex flex-wrap gap-4">
              <button 
                onClick={() => scrollToSection('projects')}
                className="flex items-center gap-2 bg-blue-600 text-white px-6 py-3 rounded-lg font-semibold hover:bg-blue-700 transition-all hover:scale-105"
              >
                View Case Studies <ChevronRight size={18} />
              </button>
              <button className={`flex items-center gap-2 px-6 py-3 rounded-lg font-semibold border ${border} hover:border-slate-500 bg-slate-900 transition-colors`}>
                Download CV <Download size={18} />
              </button>
            </div>
          </div>

          {/* Dynamic Project Spotlight */}
          <div className="relative">
            <div className={`${bgCard} p-8 rounded-2xl shadow-xl border ${border} relative z-10 transform md:rotate-2 hover:rotate-0 transition-transform duration-500 min-h-[300px] flex flex-col justify-center`}>
              
              {/* Header / Top Bar */}
              <div className="flex justify-between items-center mb-6">
                <span className={`text-xs font-bold px-3 py-1 rounded-full uppercase tracking-wide bg-slate-800 text-slate-300`}>
                  Project Spotlight
                </span>
                {/* Dots indicator */}
                <div className="flex gap-1.5">
                    {projects.map((_, idx) => (
                      <div key={idx} className={`w-2 h-2 rounded-full transition-colors duration-300 ${idx === heroProjectIndex ? 'bg-blue-600' : 'bg-slate-700'}`} />
                    ))}
                </div>
              </div>

              {/* Content with Fade Effect */}
              <div className={`transition-opacity duration-500 ease-in-out ${heroFade ? 'opacity-100' : 'opacity-0'}`}>
                <h3 className={`text-2xl font-bold ${textMain} mb-3`}>{currentHeroProject.title}</h3>
                
                <div className="flex flex-wrap gap-2 mb-4">
                    {currentHeroProject.tags.slice(0, 2).map(tag => (
                      <span key={tag} className={`text-xs ${textMuted} border ${border} px-2 py-0.5 rounded`}>
                          {tag}
                      </span>
                    ))}
                </div>

                <p className={`${textMuted} text-sm mb-6 leading-relaxed line-clamp-3`}>
                    {currentHeroProject.description}
                </p>

                <div className={`p-4 rounded-lg border ${border} bg-slate-950/50 flex items-center gap-3`}>
                    <TrendingUp size={20} className="text-green-500" />
                    <div>
                        <div className={`text-xs ${textMuted} font-semibold uppercase tracking-wider`}>Impact</div>
                        <div className={`text-sm font-bold ${textMain}`}>{currentHeroProject.metric}</div>
                    </div>
                </div>
              </div>

            </div>
            {/* Decoration */}
            <div className={`absolute -bottom-6 -right-6 w-full h-full bg-slate-800/50 rounded-2xl -z-10`}></div>
          </div>
        </div>

        {/* KPI Strip */}
        <div className="max-w-6xl mx-auto mt-20">
          <div className={`grid grid-cols-2 md:grid-cols-4 gap-8 border-y ${border} py-8`}>
            {stats.map((stat, idx) => (
              <div key={idx} className="text-center">
                <div className={`text-3xl font-bold ${textMain} mb-1`}>{stat.value}</div>
                <div className={`text-sm ${textMuted} font-medium uppercase tracking-wider`}>{stat.label}</div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* --- SKILLS SECTION --- */}
      <section id="skills" className={`py-20 px-6 ${bgCard}`}>
        <div className="max-w-6xl mx-auto">
          <div className="text-center mb-16">
            <h2 className={`text-3xl font-bold ${textMain} mb-4`}>Technical Expertise</h2>
            <p className={`${textMuted} max-w-2xl mx-auto`}>
              My toolkit focuses on transforming raw data into actionable insights, 
              utilizing industry-standard tools for visualization, coding, and analysis.
            </p>
          </div>

          <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
            {/* Category 1: Languages */}
            <div className={`p-6 bg-slate-950 border-slate-800 rounded-xl border hover:shadow-md transition-shadow`}>
              <div className={`w-12 h-12 bg-blue-900/30 rounded-lg flex items-center justify-center text-blue-600 mb-6`}>
                <Code size={24} />
              </div>
              <h3 className={`text-xl font-bold mb-4 ${textMain}`}>Languages & Code</h3>
              <ul className="space-y-3">
                <li className="flex justify-between items-center">
                  <span className={`font-medium text-slate-300`}>Python</span>
                  <span className={`text-xs ${bgCard} border ${border} px-2 py-1 rounded`}>Pandas</span>
                </li>
                 <li className="flex justify-between items-center">
                  <span className={`font-medium text-slate-300`}>R</span>
                  <span className={`text-xs ${bgCard} border ${border} px-2 py-1 rounded`}>Statistics</span>
                </li>
                <li className="flex justify-between items-center">
                  <span className={`font-medium text-slate-300`}>Java</span>
                  <span className={`text-xs ${bgCard} border ${border} px-2 py-1 rounded`}>Core</span>
                </li>
                <li className="flex justify-between items-center">
                  <span className={`font-medium text-slate-300`}>SQL</span>
                  <span className={`text-xs ${bgCard} border ${border} px-2 py-1 rounded`}>Querying</span>
                </li>
              </ul>
            </div>

            {/* Category 2: Soft Skills / Analysis */}
            <div className={`p-6 bg-slate-950 border-slate-800 rounded-xl border hover:shadow-md transition-shadow`}>
              <div className={`w-12 h-12 bg-indigo-900/30 rounded-lg flex items-center justify-center text-indigo-600 mb-6`}>
                <Brain size={24} />
              </div>
              <h3 className={`text-xl font-bold mb-4 ${textMain}`}>Analysis & Soft Skills</h3>
              <ul className="space-y-3">
                <li className="flex justify-between items-center">
                  <span className={`font-medium text-slate-300`}>Data Storytelling</span>
                  <span className={`text-xs ${bgCard} border ${border} px-2 py-1 rounded`}>Reporting</span>
                </li>
                <li className="flex justify-between items-center">
                  <span className={`font-medium text-slate-300`}>Req. Gathering</span>
                  <span className={`text-xs ${bgCard} border ${border} px-2 py-1 rounded`}>Documentation</span>
                </li>
                <li className="flex justify-between items-center">
                  <span className={`font-medium text-slate-300`}>Agile</span>
                  <span className={`text-xs ${bgCard} border ${border} px-2 py-1 rounded`}>Scrum</span>
                </li>
              </ul>
            </div>

            {/* Category 3: Visualization */}
            <div className={`p-6 bg-slate-950 border-slate-800 rounded-xl border hover:shadow-md transition-shadow`}>
              <div className={`w-12 h-12 bg-teal-900/30 rounded-lg flex items-center justify-center text-teal-600 mb-6`}>
                <PieChart size={24} />
              </div>
              <h3 className={`text-xl font-bold mb-4 ${textMain}`}>BI & Visualization</h3>
              <ul className="space-y-3">
                <li className="flex justify-between items-center">
                  <span className={`font-medium text-slate-300`}>Tableau</span>
                  <span className={`text-xs ${bgCard} border ${border} px-2 py-1 rounded`}>Dashboards</span>
                </li>
                <li className="flex justify-between items-center">
                  <span className={`font-medium text-slate-300`}>Power BI</span>
                  <span className={`text-xs ${bgCard} border ${border} px-2 py-1 rounded`}>KPIs</span>
                </li>
                <li className="flex justify-between items-center">
                  <span className={`font-medium text-slate-300`}>Data Cleaning</span>
                  <span className={`text-xs ${bgCard} border ${border} px-2 py-1 rounded`}>ETL</span>
                </li>
              </ul>
            </div>
          </div>
        </div>
      </section>

      {/* --- PROJECTS SECTION --- */}
      <section id="projects" className={`py-20 px-6 ${bgMain}`}>
        <div className="max-w-6xl mx-auto">
          <div className="flex flex-col md:flex-row justify-between items-end mb-12 gap-6">
            <div>
              <h2 className={`text-3xl font-bold ${textMain} mb-2`}>Featured Projects</h2>
              <p className={textMuted}>Case studies highlighting impact, methodology, and leadership.</p>
            </div>
            
            {/* Filter Buttons */}
            <div className={`flex gap-2 ${bgCard} p-1 rounded-lg border ${border} shadow-sm overflow-x-auto`}>
              {filterCategories.map(cat => (
                <button
                  key={cat}
                  onClick={() => setActiveFilter(cat)}
                  className={`px-4 py-2 rounded-md text-sm font-medium transition-all whitespace-nowrap ${
                    activeFilter === cat 
                      ? 'bg-blue-600 text-white shadow-md' 
                      : `${textMuted} hover:bg-slate-800`
                  }`}
                >
                  {cat}
                </button>
              ))}
            </div>
          </div>

          <div className="grid md:grid-cols-2 gap-8">
            {filteredProjects.map((project) => (
              <div key={project.id} className={`${bgCard} rounded-2xl overflow-hidden shadow-sm border ${border} hover:shadow-xl transition-shadow group flex flex-col`}>
                {/* Card Header Color Strip */}
                <div className={`h-2 w-full ${project.color}`}></div>
                
                <div className="p-8 flex-1 flex flex-col">
                  <div className="flex justify-between items-start mb-4">
                    <div className={`inline-block px-3 py-1 rounded-full bg-slate-800 text-slate-300 text-xs font-bold uppercase tracking-wide`}>
                      {project.category}
                    </div>
                    {project.link ? (
                      <a href={project.link} target="_blank" rel="noopener noreferrer" aria-label="View Project">
                        <ExternalLink size={20} className="text-slate-400 group-hover:text-blue-600 transition-colors cursor-pointer" />
                      </a>
                    ) : (
                      <ExternalLink size={20} className="text-slate-400 group-hover:text-blue-600 transition-colors cursor-pointer" />
                    )}
                  </div>
                  
                  <h3 className={`text-2xl font-bold ${textMain} mb-3`}>{project.title}</h3>
                  <p className={`${textMuted} mb-6 leading-relaxed`}>
                    {project.description}
                  </p>

                  <div className="mt-auto">
                    {/* Key Metric Highlight */}
                    <div className={`bg-slate-950 border-slate-800 p-4 rounded-lg border mb-6 flex items-center gap-3`}>
                      <TrendingUp size={20} className="text-green-600" />
                      <div>
                        <span className="block text-xs text-slate-500 font-semibold uppercase">Key Result</span>
                        <span className={`font-bold ${textMain}`}>{project.metric}</span>
                      </div>
                    </div>

                    <div className="flex flex-wrap gap-2">
                      {project.tags.map(tag => (
                        <span key={tag} className={`text-xs ${textMuted} ${bgCard} border ${border} px-2 py-1 rounded shadow-sm`}>
                          {tag}
                        </span>
                      ))}
                    </div>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* --- EXPERIENCE TIMELINE --- */}
      <section id="experience" className={`py-20 px-6 ${bgCard}`}>
        <div className="max-w-4xl mx-auto">
          <h2 className={`text-3xl font-bold ${textMain} mb-12 text-center`}>Experience & Education</h2>
          
          <div className="relative">
            {/* Vertical Line */}
            <div className={`absolute left-8 top-0 bottom-0 w-0.5 border-slate-800 bg-slate-800`}></div>

            <div className="space-y-12">
              {timeline.map((job, idx) => (
                <div key={idx} className="relative pl-24">
                  {/* Dot */}
                  <div className={`absolute left-[26px] top-2 w-4 h-4 rounded-full bg-blue-600 border-4 border-slate-900 shadow-sm z-10`}></div>
                  
                  <div className="flex flex-col md:flex-row md:items-center justify-between mb-2">
                    <h3 className={`text-xl font-bold ${textMain}`}>{job.role}</h3>
                    <span className="text-sm font-semibold text-blue-500 bg-blue-900/30 px-3 py-1 rounded-full w-fit mt-2 md:mt-0">
                      {job.period}
                    </span>
                  </div>
                  <div className={`text-lg font-medium text-slate-300 mb-2`}>{job.company}</div>
                  <p className={`${textMuted} leading-relaxed max-w-2xl`}>
                    {job.desc}
                  </p>
                </div>
              ))}
            </div>
          </div>
        </div>
      </section>

      {/* --- CTA / CONTACT --- */}
      <section id="contact" className="py-24 px-6 bg-slate-950 text-white relative overflow-hidden">
        {/* Decorative Circles */}
        <div className="absolute top-0 right-0 w-64 h-64 bg-blue-600 rounded-full blur-[100px] opacity-20 transform translate-x-1/2 -translate-y-1/2"></div>
        <div className="absolute bottom-0 left-0 w-64 h-64 bg-teal-500 rounded-full blur-[100px] opacity-20 transform -translate-x-1/2 translate-y-1/2"></div>

        <div className="max-w-4xl mx-auto text-center relative z-10">
          <h2 className="text-4xl font-bold mb-6">Let's solve your data problems.</h2>
          <p className="text-slate-300 text-lg mb-10 max-w-2xl mx-auto">
            I'm currently open to full-time opportunities. 
            If you need a dashboard built or a dataset analyzed, let's talk.
          </p>
          
          <div className="flex flex-col md:flex-row gap-6 justify-center items-center">
            <a href="mailto:thapelomohale@gmail.com" className="flex items-center gap-3 bg-blue-600 hover:bg-blue-700 text-white px-8 py-4 rounded-lg font-semibold transition-all">
              <Mail size={20} />
              thapelomohal002@gmail.com
            </a>
            <div className="flex gap-4">
              <a href="https://www.linkedin.com/in/thapelo-mohaleamalla" target="_blank" rel="noreferrer" className="p-4 bg-slate-800 rounded-lg hover:bg-slate-700 transition-colors">
                <Linkedin size={24} />
              </a>
            </div>
          </div>
        </div>
      </section>

      {/* Footer */}
      <footer className="bg-slate-950 text-slate-500 py-8 text-center text-sm border-t border-slate-900">
        <p>&copy; {new Date().getFullYear()} Thapelo Mohaleamalla. Built with React & Tailwind.</p>
      </footer>

    </div>
  );
};

export default Portfolio;
